# SCT_DA_2

I transitioned from Excel GUI to programmatic data manipulation using Python and Pandas. I built an ETL script in Google Colab to load raw data, identify and drop duplicates, handle missing values, and cast string columns into datetime objects for accurate time-series analysis.
